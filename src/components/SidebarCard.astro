---
interface Props {
  label: string;
}
const { label } = Astro.props;
---
<div class="bg-white rounded-lg px-[18px] py-4" style="border: 0.5px solid #E5E7EB;">
  <p class="text-[11px] uppercase tracking-widest text-[#9CA3AF] mb-3 font-sans">{label}</p>
  <slot />
</div>
