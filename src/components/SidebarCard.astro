---
interface Props {
  label: string;
}
const { label } = Astro.props;
---
<div class="border border-gray-200 rounded-lg p-4 mb-4">
  <p class="text-xs uppercase tracking-widest text-[var(--color-text-muted)] font-medium mb-3">{label}</p>
  <slot />
</div>
